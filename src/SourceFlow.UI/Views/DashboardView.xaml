<UserControl x:Class="SourceFlow.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:SourceFlow.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- ヘッダー -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="ダッシュボード" FontSize="24" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock Text="システム状態と最近の活動を確認できます" FontSize="14" Foreground="Gray"/>
            </StackPanel>

            <!-- システム状態カード -->
            <Border Grid.Row="1" Background="White" BorderBrush="LightGray" BorderThickness="1" 
                    CornerRadius="5" Padding="15" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="システム状態" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <WrapPanel Margin="0,0,0,10">
                            <TextBlock Text="状態: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding SystemStatus}" Foreground="DarkGreen"/>
                        </WrapPanel>
                        
                        <WrapPanel Margin="0,0,0,5">
                            <TextBlock Text="ソース設定: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding EnabledSources}"/>
                            <TextBlock Text=" / "/>
                            <TextBlock Text="{Binding TotalSources}"/>
                            <TextBlock Text=" 個が有効"/>
                        </WrapPanel>
                        
                        <WrapPanel Margin="0,0,0,5">
                            <TextBlock Text="最新同期: " FontWeight="Bold"/>
                            <TextBlock Text="{Binding LastSyncTime, StringFormat='yyyy/MM/dd HH:mm'}"/>
                            <TextBlock Text=" ("/>
                            <TextBlock Text="{Binding LastSyncStatus}"/>
                            <TextBlock Text=")"/>
                        </WrapPanel>
                    </StackPanel>
                    
                    <Button Grid.Column="1" Content="更新" Command="{Binding RefreshCommand}" 
                            Width="80" Height="30" Margin="10,0,0,0"/>
                </Grid>
            </Border>

            <!-- メインコンテンツ -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 左側: クイックアクション -->
                <GroupBox Grid.Column="0" Header="クイックアクション" Margin="0,0,0,0">
                    <ItemsControl ItemsSource="{Binding QuickActions}" Margin="5">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="LightBlue" BorderBrush="DarkBlue" BorderThickness="1" 
                                        CornerRadius="3" Margin="0,0,0,5" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                            <TextBlock Text="{Binding Description}" FontSize="12" 
                                                       Foreground="DarkBlue" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        
                                        <Button Grid.Column="1" Content="実行" 
                                                Command="{Binding DataContext.ExecuteQuickActionCommand, 
                                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" 
                                                Width="60" Height="25" Margin="10,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- 右側: 最近のジョブ -->
                <GroupBox Grid.Column="2" Header="最近の同期ジョブ">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <ListView Grid.Row="0" ItemsSource="{Binding RecentJobs}" Margin="5">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="ジョブ名" Width="120">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding JobName}" TextWrapping="Wrap"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="開始時刻" Width="120">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding StartTime, StringFormat='MM/dd HH:mm'}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="状態" Width="60">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="ファイル数" Width="80">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding FilesCount}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        
                        <Button Grid.Row="1" Content="すべて表示" Command="{Binding ViewAllJobsCommand}" 
                                HorizontalAlignment="Right" Width="80" Height="25" Margin="5"/>
                    </Grid>
                </GroupBox>
            </Grid>

            <!-- ローディング表示 -->
            <Border Grid.RowSpan="3" Background="Black" Opacity="0.5" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                    <TextBlock Text="読み込み中..." Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>